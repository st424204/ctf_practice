The kernel of this vm is vulnerable to CVE-2015-8966, exploit it to 
gain root privileges and read /flag/flag.txt !

The file vmlinux is the kernel running on the vm with debug symbols.
The file config is the .config used to compile the kernel.

